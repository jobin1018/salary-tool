services:
  - type: web
    name: salary-tool-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./salary_tool.db
      - key: PORT
        value: 8000
